# nanoCursor 项目进化规划

## 当前状态

nanoCursor 是一个功能完整的 AI 自动编程框架：Agent Loop、多 Agent 团队协作、跨会话记忆、任务 DAG、可观测性等基础模块都已就绪。

**核心问题**：
1. 前端过重：React + FastAPI + SSE 维护成本高，与目标用户（终端开发者）的使用场景不匹配
2. 定位模糊：功能很多但缺乏明确主线

**核心目标**：把 nanoCursor 从"框架 demo"变成"自己能天天用的 CLI 工具"，在真实使用中自然生长出特色。

---

## 总体路线

```
Phase 1 (CLI MVP)     Phase 2 (特色暴露)     Phase 3 (体验打磨)     Phase 4 (自举)
   2-3 周                2-3 周                   持续                   持续
┌──────────────┐    ┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│ 砍前端        │ ──▶│ Team 命令     │  ──▶  │ 流式输出      │  ──▶  │ 用 nanoCursor │
│ CLI REPL     │    │ Memory 感知   │       │ 会话管理      │       │ 开发 nanoCursor│
│ 基础交互      │    │ Task 可视化   │       │ 配置优化      │       │ 持续迭代      │
└──────────────┘    └──────────────┘       └──────────────┘       └──────────────┘
```

---

## Phase 1: CLI MVP — "能用"

**目标**：砍掉前端，做一个在终端里能跑的命令行 Agent。

### 核心改造
- `prompt_toolkit` 替代 `input()`：多行输入、历史记录、自动补全
- `Rich` 渲染 markdown / 代码块 / 表格 / 实时流式输出
- 斜杠命令系统：`/help`, `/files`, `/config`, `/metrics`, `/clear` 等

### 目录变化
```
nanoCursor/
├── cli.py                 # 新增：CLI 主入口
├── src/cli/               # 新增：CLI 模块
│   ├── repl.py            # REPL 循环 + prompt_toolkit
│   ├── renderer.py        # Rich 渲染器
│   └── commands.py        # 斜杠命令
├── _archive/frontend/     # 归档的前端
└── ... (其他保持不变)
```

### 复用的现有模块（不动）
- `src/agent/engine.py` - agent_loop, TOOL_HANDLERS, SystemPromptBuilder
- `src/infra/llm_config.py` - create_client, MODEL
- `src/infra/metrics.py` - MetricsCollector
- `src/memory/manager.py` - MemoryManager
- `src/team/team.py` - TeammateManager

---

## Phase 2: 特色暴露 — "与众不同"

**目标**：把 Team / Memory / Task 能力在 CLI 中自然暴露。

### Team 命令
```
/team spawn review-agent "审查专家" "审查所有变更"
/team list
/team send review-agent "请审查 commit 35ca591"
/team inbox
/team shutdown review-agent
```

### Memory 感知
Agent 在对话开始时自动加载相关记忆，自然引用。

```
/memory save "测试必须连真实数据库"
/memory search "数据库"
/memory list
```

### Task DAG 可视化
```
/task create "重构 engine.py" --blocks "001,002"
/task list
/task graph
```

---

## Phase 3: 体验打磨 — "好用"

- **流式输出**：agent_loop 改为 generator 模式，逐 token 输出
- **会话管理**：`/session list`, `/session resume`
- **配置体验**：`/config set model deepseek-v3` 热切换
- **Agent 增强**：tiktoken 精确计数、错误恢复增强、Docker 沙盒

---

## Phase 4: 自举 & 持续迭代 — "吃自己的狗粮"

- 用 nanoCursor 开发 nanoCursor（50%+ 编码量由 nanoCursor 完成）
- 每个 Phase 后用 nanoCursor 审查自己的代码
- 方向在使用中自然浮现

---

## 不做的

- ❌ 不重构现有核心模块
- ❌ 不做复杂前端 UI
- ❌ 不追求大而全的工具集
- ❌ 不设计完美架构，用着改着

---

## 版本里程碑

| 版本 | 标志 | 说明 |
|------|------|------|
| v0.1 | CLI MVP | 终端里交互式编程 |
| v0.2 | 特色暴露 | Team/Memory/Task 命令可用 |
| v0.3 | 体验打磨 | 流式输出 + 会话 + 热切换 |
| v0.4+ | 持续迭代 | 自举开发 |
