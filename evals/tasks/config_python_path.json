{
  "id": "config_python_path",
  "title": "Configure Python Path",
  "fixture": "config_python_path",
  "prompt": "The project needs a .env file with PYTHONPATH set to the src directory. Create it.",
  "category": "config_mcp_skill",
  "difficulty": "easy",
  "timeout_seconds": 120,
  "test_command": "grep -q 'PYTHONPATH' .env",
  "expected_signals": {
    "required_events": [
      "plan_created",
      "file_changed",
      "done"
    ],
    "tests_pass": true,
    "max_changed_files": 2,
    "required_files": [
      ".env"
    ]
  }
}